import React from 'react';
import {Toolbar} from '../../../aggregate/App/styled';

export default function RootToolbar() {
  return <Toolbar style={{fontFamily: 'GOST type B'}}>ProductStructure</Toolbar>
}
