import React from 'react';
import PropTypes from 'prop-types';

export default function TextWnd({editor}) {
  return <div>
    Text
  </div>;
}

TextWnd.propTypes = {
  editor: PropTypes.object.isRequired,
};
