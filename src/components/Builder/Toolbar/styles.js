
import {withStyles} from '@material-ui/core/styles';

export const useStyles = withStyles({
  title: {
    flexGrow: 1,
  },
  sp: {
    paddingLeft: 8,
  },
  toggleBtn: {
    marginLeft: 8,
    color: 'inherit',
    width: 40,
  }
});
