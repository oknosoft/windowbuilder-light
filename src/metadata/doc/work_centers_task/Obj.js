import React from 'react';
import Typography from '@mui/material/Typography';
import {Root} from '../calc_order/styled';

export default function WorkCentersTaskObj(props) {
  return <Root>
    <Typography variant="h3">
      WorkCentersTaskObj
    </Typography>
    <Typography>
      Здесь будет задание на производство
    </Typography>
  </Root>;
}
